// App Config
// ---------------
// The initial configuration of our AngularJS app.<br/>
// We don't do any routing here as we delegate views switching to Polymer's
// `<core-animated-pages>` element.
angular.module('Androguide', ['ngAnimate']).config([function() {

}]);